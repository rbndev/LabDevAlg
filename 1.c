#include <stdio.h>

// PLANEJAMENTO => DECLARAR VARIAVEL COMO INTEIRO, LER O NUMERO E EXIBIR O NUMERO.

int main(void) 
{
  int numero;
  scanf("%d", &numero);
  printf("o número digita foi: %d\n", numero);
  return 0;
}